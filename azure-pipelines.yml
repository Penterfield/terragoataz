# azure-pipelines.yml

trigger: 
- master

# pr:
# - checkov

pool:
  vmImage: 'ubuntu-latest'
  
stages:

  - stage: Scan
    jobs:
    - job: 
      steps:
      - template: templates/checkov.yml
      
    
