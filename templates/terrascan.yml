steps:
- script: |
    mkdir TerrascanReport
    docker pull accurics/terrascan:latest
    docker run --rm -t -v $(System.DefaultWorkingDirectory):/src accurics/terrascan
  displayName: Terrasca Static Code Analysis
 
  
